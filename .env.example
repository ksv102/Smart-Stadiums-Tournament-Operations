# Copy this file to .env and fill in your key.
# ORION runs fine without a key (falls back to deterministic templated
# explanations) — the key only upgrades the natural-language narration.
ANTHROPIC_API_KEY=
PORT=3000
